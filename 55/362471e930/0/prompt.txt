There are multiple places in the code (in hooks) which operate on the file tree (i.e. LocalFileGroup). Is it possible to simplify this code by introducing more props to the LocalFileGroup data structure? I'm thinking about a `path` prop for LocalFileGroup or something like that. Or would it help to encode more information in SelectionItem maybe?

---

yes

---

What's your reasoning to name the prop `key`? Isn't it rather a `path`, like in LocalFile?

---

The return type of ScanLocalRoot is filetree.LocalFileGroup according to the generated types. How does wails know to use this type?

---

Can't we use the type generated by wails instead of hand-rolling the ScannedGroup type?

---

yes

---

Can't you just import the returned type instead of using ReturnType?

---

How about directly using filetree.LocalFileGroup instead of assigning it to ScannedGroup?

---

commit

---

tell me the contents of GITGUI_MSG