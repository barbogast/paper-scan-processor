whenever the user selects a page automatically prefetch the following and the previous page (for the Detail panel). Whenever a PDF file is opened, prefetch the second page.

---

commit

---

After the merge operation finishes, offer the user to either:
- open the file in MacOS preview
- open the file in SplitMode

---

/statusline show model name and context percentage with a progress bar

---

Keep the modal open when the user chooses "Open in Preview".
Also, "Open in Preview" is wrong on other platforms. Is there is simple way to know the name of the default app? Otherwise choose a platform-independent label for the button

---

Catch errors when SplitMode loads the PageCount

---

commit

---

Is it best practice to name functions exported by Wails PacscalCase? It looks weird in the TS code.

---

Reorganise the TODO-list for SplitMode in spec.md, similarly to MergeMode:
1. Add split button between the pages, no drag and drop yet
2. implement splitting
3. allow to set the filename for each split
4. prefill filenames
5. allow to choose output folders
6. deleting / repositioning dividers via drag and drop6.

---

Reorganise the TODO-list for SplitMode in spec.md, similarly to MergeMode:
1. Add split button between the pages, no drag and drop yet
2. implement splitting
3. allow to set the filename for each split
4. prefill filenames
5. allow to choose output folders
6. deleting / repositioning dividers via drag and drop6.
7. rotate pages
8. skip pages
9. reorder pages
10. conflict check, keep/move/delete prompt after export

Did I miss anything?

---

yes

---

commit